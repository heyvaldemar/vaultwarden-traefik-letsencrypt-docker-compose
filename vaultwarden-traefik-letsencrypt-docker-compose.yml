# Image pins live HERE, as interpolation defaults, so `git pull` (or
# re-downloading this file) delivers the exact version combination this
# repository has tested — no .env editing required. Setting the matching
# variable in .env overrides the default for anyone who wants to pin
# differently; .env itself carries only secrets and deliberate overrides.
# CI (Trivy scans, check-pin-freshness, the ephemeral test .env) reads
# the pins from these lines — this block is the single source of truth.
x-images:
  traefik: &traefik-image ${TRAEFIK_IMAGE_TAG:-traefik:${TRAEFIK_IMAGE_VERSION:-3.7@sha256:9c2a54d87f76f5c2f5f2682c68394af92fb12c0a2686798d6462a3f84bd78eaf}}
  vaultwarden: &vaultwarden-image ${VAULTWARDEN_IMAGE_TAG:-vaultwarden/server:${VAULTWARDEN_IMAGE_VERSION:-1.37.2@sha256:094b5689ed81549bd293418395c7cf495ae9d960fc2d4928cef2083ef913d912}}

networks:
  vaultwarden-network:
    external: true
  traefik-network:
    external: true

volumes:
  vaultwarden-data:
  traefik-certificates:
  vaultwarden-backups:

services:
  vaultwarden:
    image: *vaultwarden-image
    # Hardening: no privilege escalation via setuid binaries
    security_opt:
      - no-new-privileges:true
    # Resource limits as .env-overridable defaults (e.g. VAULTWARDEN_MEMORY_LIMIT=2g);
    # the override survives every git pull.
    deploy:
      resources:
        limits:
          memory: ${VAULTWARDEN_MEMORY_LIMIT:-512m}
          cpus: "${VAULTWARDEN_CPU_LIMIT:-1.0}"
        reservations:
          memory: ${VAULTWARDEN_MEMORY_RESERVATION:-64m}
          cpus: "${VAULTWARDEN_CPU_RESERVATION:-0.25}"
    volumes:
      - vaultwarden-data:/data
    environment:
      WEBSOCKET_ENABLED: 'true'
      SIGNUPS_ALLOWED: ${VAULTWARDEN_SIGNUPS_ALLOWED:-true}
    networks:
      - vaultwarden-network
      - traefik-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 90s
    labels:
      # Enable Traefik for this container
      - "traefik.enable=true"
      # Match incoming requests on a specific hostname
      - "traefik.http.routers.vaultwarden.rule=Host(`${VAULTWARDEN_HOSTNAME:?set in .env - see .env.example}`)"
      # Assign the router to a named Traefik service
      - "traefik.http.routers.vaultwarden.service=vaultwarden"
      # Use the 'websecure' (HTTPS) entry point
      - "traefik.http.routers.vaultwarden.entrypoints=websecure"
      # Define the internal container port for routing
      - "traefik.http.services.vaultwarden.loadbalancer.server.port=80"
      # Enable TLS on this router
      - "traefik.http.routers.vaultwarden.tls=true"
      # Use Let's Encrypt for certificate management
      - "traefik.http.routers.vaultwarden.tls.certresolver=letsencrypt"
      # Pass the original Host header to the container
      - "traefik.http.services.vaultwarden.loadbalancer.passhostheader=true"
      # Apply a compression middleware
      - "traefik.http.routers.vaultwarden.middlewares=compresstraefik"
      # Define settings for the compression middleware
      - "traefik.http.middlewares.compresstraefik.compress=true"
      # Specify which Docker network Traefik should use for routing
      - "traefik.docker.network=traefik-network"
    restart: unless-stopped
    depends_on:
      traefik:
        condition: service_healthy

  traefik:
    image: *traefik-image
    # Hardening: no privilege escalation via setuid binaries
    security_opt:
      - no-new-privileges:true
    # Drop every Linux capability; add back only what this image's
    # entrypoint needs (bind :80/:443, chown its data directory, drop to
    # its service user).
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    # Resource limits as .env-overridable defaults (e.g. TRAEFIK_MEMORY_LIMIT=2g);
    # the override survives every git pull.
    deploy:
      resources:
        limits:
          memory: ${TRAEFIK_MEMORY_LIMIT:-256m}
          cpus: "${TRAEFIK_CPU_LIMIT:-0.5}"
        reservations:
          memory: ${TRAEFIK_MEMORY_RESERVATION:-64m}
          cpus: "${TRAEFIK_CPU_RESERVATION:-0.1}"
    command:
      # Set the log level (DEBUG, INFO, WARN, ERROR)
      - "--log.level=${TRAEFIK_LOG_LEVEL:-WARN}"
      # Enable the built-in API and web-based dashboard
      - "--api.dashboard=true"
      # Enable the /ping endpoint so we can health-check Traefik
      - "--ping=true"
      # Assign the /ping endpoint to a dedicated entry point on port 8082
      - "--ping.entrypoint=ping"
      - "--entrypoints.ping.address=:8082"
      # Define the primary HTTP entry point on port 80
      - "--entrypoints.web.address=:80"
      # Define the secure (HTTPS) entry point on port 443
      - "--entrypoints.websecure.address=:443"
      # HTTP -> HTTPS redirect at entrypoint level
      - "--entrypoints.web.http.redirections.entrypoint.to=websecure"
      - "--entrypoints.web.http.redirections.entrypoint.scheme=https"
      # Enable the Docker provider to detect containers and their labels
      - "--providers.docker=true"
      # Point Traefik to the Docker socket
      - "--providers.docker.endpoint=unix:///var/run/docker.sock"
      # Prevent automatic exposure of all containers; only expose containers
      # with "traefik.enable=true"
      - "--providers.docker.exposedbydefault=false"
      # Use ACME (Let's Encrypt) to generate/renew certificates via TLS challenge
      - "--certificatesresolvers.letsencrypt.acme.tlschallenge=true"
      # The email address used by Let's Encrypt for renewal notices
      - "--certificatesresolvers.letsencrypt.acme.email=${TRAEFIK_ACME_EMAIL:?set in .env - see .env.example}"
      # The file where ACME certificates are stored inside the container
      - "--certificatesresolvers.letsencrypt.acme.storage=/etc/traefik/acme/acme.json"
      # Enable Prometheus metrics
      - "--metrics.prometheus=true"
      # Configure Prometheus histogram buckets
      - "--metrics.prometheus.buckets=0.1,0.3,1.2,5.0"
      # Check for newer Traefik versions and optionally log that info
      - "--global.checknewversion=true"
      # Disable sending anonymous usage data to the Traefik maintainers
      - "--global.sendanonymoususage=false"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - traefik-certificates:/etc/traefik/acme
    networks:
      - traefik-network
    ports:
      - "80:80"
      - "443:443"
    healthcheck:
      test: ["CMD", "wget", "http://localhost:8082/ping","--spider"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s
    labels:
      # Enable Traefik for this container
      - "traefik.enable=true"
      # A router to expose the Traefik dashboard
      - "traefik.http.routers.dashboard.rule=Host(`${TRAEFIK_HOSTNAME:?set in .env - see .env.example}`)"
      - "traefik.http.routers.dashboard.entrypoints=websecure"
      - "traefik.http.routers.dashboard.tls=true"
      - "traefik.http.routers.dashboard.tls.certresolver=letsencrypt"
      - "traefik.http.routers.dashboard.service=api@internal"
      # Basic Authentication for the Traefik dashboard
      - "traefik.http.routers.dashboard.middlewares=authtraefik"
      - "traefik.http.middlewares.authtraefik.basicauth.users=${TRAEFIK_BASIC_AUTH:?set in .env - see .env.example for htpasswd generation}"
      # Specify the internal server port to the dashboard service
      - "traefik.http.services.dashboard.loadbalancer.server.port=8080"
      # Pass the original Host header to the backend
      - "traefik.http.services.dashboard.loadbalancer.passhostheader=true"
    restart: unless-stopped

  backups:
    image: python:3.13-alpine@sha256:7415fbc3c9e4979cc717d92377ab2bc7b2b4a2af1ac03cc52b5f3f88efedaf3a
    # Hardening: no privilege escalation via setuid binaries
    security_opt:
      - no-new-privileges:true
    # Drop every Linux capability; add back only what this image's
    # entrypoint needs (bind :80/:443, chown its data directory, drop to
    # its service user).
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
    # Resource limits as .env-overridable defaults (e.g. VAULTWARDEN_BACKUPS_MEMORY_LIMIT=2g);
    # the override survives every git pull.
    deploy:
      resources:
        limits:
          memory: ${VAULTWARDEN_BACKUPS_MEMORY_LIMIT:-256m}
          cpus: "${VAULTWARDEN_BACKUPS_CPU_LIMIT:-0.5}"
        reservations:
          memory: ${VAULTWARDEN_BACKUPS_MEMORY_RESERVATION:-32m}
          cpus: "${VAULTWARDEN_BACKUPS_CPU_RESERVATION:-0.1}"
    # Every cycle: a consistent online copy of each SQLite database through
    # Python's backup API (no application stop), then a tar.gz of the rest of
    # the data directory (live database files excluded - the copies above are
    # the consistent ones). Logs OK/FAILED per artefact; a failed archive is
    # kept as .failed for diagnosis; retention 0 disables pruning.
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        sleep "$$VAULTWARDEN_BACKUP_INIT_SLEEP"
        while true; do
          STAMP="$$(date +%Y-%m-%d_%H-%M)"
          for db in db.sqlite3; do
            DB_FILE="$$VAULTWARDEN_BACKUPS_PATH/$$VAULTWARDEN_BACKUP_NAME-$${db%%.*}-$$STAMP.sqlite3"
            echo "[$$(date -Iseconds)] Starting database backup of $$db to $$DB_FILE.gz"
            if [ ! -f "/data/$$db" ]; then
              echo "[$$(date -Iseconds)] Database $$db not present yet (first start?) - skipped this cycle"
            elif python3 -c "import sqlite3,sys; s=sqlite3.connect('file:'+sys.argv[1]+'?mode=ro',uri=True); d=sqlite3.connect(sys.argv[2]); s.backup(d); d.close(); s.close()" "/data/$$db" "$$DB_FILE" && gzip -f "$$DB_FILE"; then
              echo "[$$(date -Iseconds)] Database backup OK: $$DB_FILE.gz ($$(stat -c %s "$$DB_FILE.gz") bytes)"
            else
              echo "[$$(date -Iseconds)] Database backup FAILED - $$db could not be copied consistently" >&2
              rm -f "$$DB_FILE" "$$DB_FILE.gz"
            fi
          done
          DATA_FILE="$$VAULTWARDEN_BACKUPS_PATH/$$VAULTWARDEN_DATA_BACKUP_NAME-$$STAMP.tar.gz"
          echo "[$$(date -Iseconds)] Starting data backup to $$DATA_FILE"
          tar -C /data --exclude='./db.sqlite3' --exclude='./db.sqlite3-wal' --exclude='./db.sqlite3-shm' --exclude='./tmp' -zcpf "$$DATA_FILE" .; RC=$$?
          # BusyBox tar exits 1 for everything from "file changed" to "cannot
          # write", so the exit code proves nothing: verify the archive itself.
          if [ "$$RC" -le 1 ] && [ -f "$$DATA_FILE" ] && gzip -t "$$DATA_FILE" 2>/dev/null; then
            echo "[$$(date -Iseconds)] Data backup OK: $$DATA_FILE ($$(stat -c %s "$$DATA_FILE") bytes)"
          else
            echo "[$$(date -Iseconds)] Data backup FAILED - partial file kept as $$DATA_FILE.failed for diagnosis" >&2
            mv "$$DATA_FILE" "$$DATA_FILE.failed" 2>/dev/null || true
          fi
          if [ "$${VAULTWARDEN_BACKUP_PRUNE_DAYS:-0}" -ge 1 ]; then
            find "$$VAULTWARDEN_BACKUPS_PATH" -type f \( -name "$$VAULTWARDEN_BACKUP_NAME-*" -o -name "$$VAULTWARDEN_DATA_BACKUP_NAME-*" \) -mtime "+$$VAULTWARDEN_BACKUP_PRUNE_DAYS" -delete
          fi
          sleep "$$VAULTWARDEN_BACKUP_INTERVAL"
        done
    volumes:
      - vaultwarden-data:/data
      - vaultwarden-backups:${VAULTWARDEN_BACKUPS_PATH:-/srv/vaultwarden/backups}
    environment:
      VAULTWARDEN_BACKUP_INIT_SLEEP: ${VAULTWARDEN_BACKUP_INIT_SLEEP:-30m}
      VAULTWARDEN_BACKUP_INTERVAL: ${VAULTWARDEN_BACKUP_INTERVAL:-24h}
      VAULTWARDEN_BACKUP_PRUNE_DAYS: ${VAULTWARDEN_BACKUP_PRUNE_DAYS:-7}
      VAULTWARDEN_BACKUPS_PATH: ${VAULTWARDEN_BACKUPS_PATH:-/srv/vaultwarden/backups}
      VAULTWARDEN_BACKUP_NAME: ${VAULTWARDEN_BACKUP_NAME:-vaultwarden-database-backup}
      VAULTWARDEN_DATA_BACKUP_NAME: ${VAULTWARDEN_DATA_BACKUP_NAME:-vaultwarden-data-backup}
    restart: unless-stopped
